# Фрагмент для nginx на VPS: TLS (certbot) + прокси на uvicorn.
# Замените fb.example.com и пути к сертификатам.

upstream fb_master_app {
    server 127.0.0.1:8000;
    keepalive 32;
}

server {
    listen 443 ssl http2;
    server_name fb.example.com;

    ssl_certificate     /etc/letsencrypt/live/fb.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/fb.example.com/privkey.pem;

    client_max_body_size 64m;

    location / {
        proxy_pass http://fb_master_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}

server {
    listen 80;
    server_name fb.example.com;
    return 301 https://$host$request_uri;
}
